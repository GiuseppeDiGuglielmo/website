---
import Layout from '../../layouts/Layout.astro';
import profile from '../../data/profile.json';
---

<Layout
  title="AI Hardware & ML-to-Silicon Projects | Giuseppe Di Guglielmo"
  description="Flagship engineering projects in AI hardware, ASIC, and FPGA design."
>
  <h1>Selected Work</h1>

  <!-- TODO: per plan.md §8-9 — each project card should link to a case
       study page structured as Problem / My Role / Technical Approach /
       Results / Technologies / Related Publications. -->
  <ul>
    {profile.projects.map((p) => (
      <li>
        <a href={`/projects/${p.slug}/`}>{p.name}</a> — {p.oneLiner}
      </li>
    ))}
  </ul>
</Layout>
